source = /home/all/.config/hypr/autostart.conf
$mod = SUPER 

monitor=,preferred,auto,auto

$terminal = alacritty
$fileManager = dolphin
$menu = rofi -show drun


bind = $mod, RETURN, exec, $terminal
bind = $mod SHIFT, Q, killactive,
bind = $mod, E, exec, $fileManager
bind = $mod, F, fullscreen,
bind = $mod, SPACE, exec, $menu
bind = $mod, X, exec, wlogout
bind = $mod, V, togglefloating, 
bind = $mod, P, pin

bind = $mod, T, exec, ~/.config/rofi/bin/rofi-hue.sh
bind = $mod SHIFT, T, exec, ~/.config/rofi/bin/random-hue.sh

# Permite mover a janela segurando $mainMod + Botão Esquerdo (LMB)
bindm = $mod, mouse:272, movewindow

# Permite redimensionar a janela segurando $mainMod + Botão Direito (RMB)
bindm = $mod, mouse:273, resizewindow

bind = $mod, H, movefocus, l
bind = $mod, L, movefocus, r
bind = $mod, K, movefocus, u
bind = $mod, J, movefocus, d

# Switch workspaces with mod + [0-9]
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

# Move active window to a workspace with mod + SHIFT + [0-9]
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10


# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# Ativar bordas
general {
    border_size = 1
    gaps_out = 10
    gaps_in = 3
}

# Arredondamento das bordas das janelas
decoration {
    rounding = 6  # aumenta ou diminui o valor para ajustar o quanto arredondado
    blur {
        enabled = true
        size = 1
        passes = 3
        new_optimizations = true
    }
    inactive_opacity = 0.8
}

# HyprMod managed settings
source = /home/all/.config/hypr/hyprland-gui.conf
